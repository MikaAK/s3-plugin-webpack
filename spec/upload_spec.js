import testHelpers from './upload_test_helpers'

describe('S3 Webpack Upload', function() {
  it('uploads entire directory to s3 if directory is defined')
  it('uploads build to s3 if no directory option')
  it('doesn\'t upload excluded')
  it('cdnizes links inside of html files')
})
